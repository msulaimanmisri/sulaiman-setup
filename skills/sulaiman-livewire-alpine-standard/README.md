## Installation

Use this option when you want the skill installed in the shared agent directory instead of a provider-specific directory.

```bash
npx skills add msulaimanmisri/sulaiman-setup --skill sulaiman-livewire-alpine-standard
```

Installed into:

```text
.agents/skills/sulaiman-livewire-alpine-standard/
```
