# documentation
- For long README files, add a table of contents / navigation menu at the top so users can click to jump to sections instead of scrolling. Confidence: 0.70
- Keep README files slim by referencing separate source files (e.g., bashrc, zshrc, .mjs) instead of embedding large code blocks inline. Tell users to open those files to see the code. Confidence: 0.70

# workflow
- When code review comments are shared, evaluate them critically — push back if a suggestion doesn't make sense rather than implementing it blindly. Confidence: 0.70
