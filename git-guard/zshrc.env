# Git Guard - By Sulaiman Misri
export GIT_GUARD_ALLOWED_BRANCH=staging # Change this to your selected branch name

git() {
  if [ "$1" = "pull" ]; then
    shift
    node ~/.git-guard/git-pull-guard.mjs "$@"
    return $?
  fi

  command git "$@"
}