# >>> Git-guard by Sulaiman Misri >>>
export GIT_GUARD_ALLOWED_BRANCH=staging
git() {
  if [ "$1" = "pull" ]; then
    shift
    git-guard "$@"
    return $?
  fi

  command git "$@"
}
# <<< Git-guard <<<
